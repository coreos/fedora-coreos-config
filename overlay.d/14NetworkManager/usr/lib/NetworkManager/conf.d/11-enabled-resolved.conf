# Instruct Network Manager to update and use systemd-resolved.
# These settings are needed to set the hostname from DNS.
[main]

# Send the connection DNS configuration to systemd-resolved.
systemd-resolved=true

# Use systemd-resolved for name resolution.
dns=systemd-resolved

